<br>
<div class="new_group_form">
<h1>New Group</h1>
<br>
<%= form_with(model: @group) do |form| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
        <% @group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>
  <div class="field">
    <%= form.label :group_name %>
    <%= form.text_field :group_name %>
  </div>
    <br><br>
  <div class="actions">
    <%= form.button "Add new group", type: "submit", :class=> "btn btn-primary", :style=> "color:white !important" %>
  </div>
<% end %>
<%= link_to 'Back', '/admin', :class=> "btn btn-secondary", :style=> "color:white" %>
</div>
<style scoped>
.new_group_form {
  display: flex;
  justify-content: space-between;
  flex-direction: column;
  align-items: flex-start;
  margin-left: 40%;
}
.new_group_form button {
  color: white !important;
}
.new_group_form h1 {
  margin-left:0;
}
</style>